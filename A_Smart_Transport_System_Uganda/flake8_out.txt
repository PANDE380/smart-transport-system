Previous F811 entries in backend/app.py were from an older file state and are
no longer reproducible after the backend import cleanup.

Current verification status on 2026-03-22:
- root route returns 200
- /api/health returns 200
- registration, login, wallet, top-up, fare estimate, booking, payment,
  history, admin dashboard, vehicles, and USSD flows all return expected
  status codes
- invalid payload checks return clean 400 responses instead of server errors
